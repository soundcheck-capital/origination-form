const StepTitle = ({ title }: { title: string }) => {
    return (
        <p className=" text-amber-500 my-4 text-3xl font-bold text-center lg:w-[30%] mx-auto">{title}</p>
    );
};

export default StepTitle;