const StepTitle = ({ title }: { title: string }) => {
    return (
        <p className=" text-amber-500 my-4 text-2xl font-semibold text-center mx-auto">{title}</p>
    );
};

export default StepTitle;