const StepTitle = ({ title }: { title: string }) => {
    return (
        <p className="text-xl text-amber-500 mb-8 text-2xl font-400 mt-8 text-center">{title}</p>
    );
};

export default StepTitle;